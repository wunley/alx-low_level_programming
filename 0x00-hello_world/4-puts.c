#include <stdio.h>
/**
 * main - Entry point
 * Return - Always0 (Success)
 */

int main(void)
{
	char progi[] = "Programming is like building multilingual puzzle";

        puts(progi);

        return (0);
}

