#include <stdio.h>
/**
 * main - Displays base 10 numbers
 * Return: Always 0 (success)
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%i", n);
		n++;
	}
	printf('\n');
	return (0);
}
